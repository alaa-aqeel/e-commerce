version: '3.8'

services:
  ecommerce-api-tests:
    build:
      context: .
      dockerfile: Dockerfile.test
    container_name: ecommerce_api_tests
    volumes:
      - ./TestResults:/src/ECommerceApi.Tests/TestResults
